# 🌐 网站部署完整指南

## ✨ 推荐方案：Vercel（最简单）

### 为什么选择Vercel？
- ✅ 完全免费
- ✅ 自动部署（推送代码即更新）
- ✅ 全球CDN，速度快
- ✅ 免费HTTPS证书
- ✅ 支持自定义域名

---

## 📋 部署步骤（5分钟搞定）

### 第一步：准备GitHub仓库

1. **在GitHub创建新仓库**
   - 访问 https://github.com/new
   - 仓库名：例如 `personal-website`
   - 选择 Public（公开）
   - 点击 "Create repository"

2. **在本地初始化Git并推送代码**

   打开项目文件夹，在终端执行：

   ```bash
   # 初始化Git（如果还没有）
   git init

   # 添加所有文件
   git add .

   # 提交代码
   git commit -m "初始提交"

   # 连接GitHub仓库（替换成你的仓库地址）
   git remote add origin https://github.com/你的用户名/你的仓库名.git

   # 推送到GitHub
   git branch -M main
   git push -u origin main
   ```

### 第二步：部署到Vercel

1. **访问Vercel**
   - 打开 https://vercel.com
   - 点击 "Sign Up" 使用GitHub账号登录

2. **导入项目**
   - 登录后点击 "Add New..." -> "Project"
   - 选择你刚创建的GitHub仓库
   - 点击 "Import"

3. **配置项目**
   - **Framework Preset**: 选择 "Vue"
   - **Build Command**: `npm run build`（自动识别）
   - **Output Directory**: `dist`（自动识别）
   - 其他保持默认

4. **部署**
   - 点击 "Deploy"
   - 等待1-2分钟
   - 完成！🎉

5. **获取网站地址**
   - 部署完成后，会显示你的网站地址
   - 例如：`https://your-site.vercel.app`
   - 这个地址就是你的个人网站！

---

## 🔄 如何更新网站内容

### 方法一：通过Git推送（推荐）

1. **修改代码**
   - 在本地编辑器中修改文件
   - 例如：修改 `src/components/sections/AboutSection.vue` 中的个人信息

2. **提交并推送**
   ```bash
   # 添加修改的文件
   git add .

   # 提交更改
   git commit -m "更新个人信息"

   # 推送到GitHub
   git push
   ```

3. **自动部署**
   - Vercel会自动检测到代码更新
   - 自动开始构建和部署（1-2分钟）
   - 完成后网站自动更新！

### 方法二：通过Vercel Dashboard在线编辑（不推荐）

- 也可以直接在GitHub网页上编辑文件，效果相同

---

## 🎨 其他部署平台

### Netlify（同样简单）

1. 访问 https://www.netlify.com
2. 使用GitHub登录
3. 点击 "Add new site" -> "Import an existing project"
4. 选择你的GitHub仓库
5. 构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击 "Deploy site"

**更新方式**：同Vercel，推送代码到GitHub即可自动部署

### GitHub Pages（免费但需要配置）

1. 修改 `vite.config.js`，添加：
   ```js
   base: '/仓库名/',
   ```

2. 推送到GitHub后，在仓库设置中启用GitHub Pages
3. 网站地址：`https://你的用户名.github.io/仓库名/`

---

## 🌍 绑定自定义域名（可选）

### Vercel绑定域名

1. 在Vercel项目设置中找到 "Domains"
2. 输入你的域名（如：gantu.site）
3. 按照提示配置DNS记录
4. 完成后，你的网站就可以通过自定义域名访问了

**免费域名购买推荐**：
- Namecheap（约$1/年）
- Cloudflare（域名注册+免费DNS）
- Freenom（完全免费，但限制较多）

---

## 📝 常用修改场景

### 修改个人信息
- 编辑文件：`src/components/sections/AboutSection.vue`
- 修改第43-53行的数据

### 修改项目经历
- 编辑文件：`src/components/sections/ProjectsSection.vue`
- 修改 `projects` 数组中的数据

### 修改教育背景
- 编辑文件：`src/components/sections/EducationSection.vue`
- 修改 `courses` 数组

### 修改首页内容
- 编辑文件：`src/components/sections/HeroSection.vue`
- 修改打字机效果的文字（第78-83行）

### 修改联系方式
- 编辑文件：`src/components/sections/ContactSection.vue`
- 修改 `contactItems` 数组

---

## ⚠️ 注意事项

1. **每次修改后记得推送**
   ```bash
   git add .
   git commit -m "描述你的修改"
   git push
   ```

2. **本地预览修改**
   ```bash
   npm run dev
   ```
   在浏览器查看 `http://localhost:3000` 确认无误后再推送

3. **查看部署状态**
   - Vercel Dashboard会显示每次部署的状态
   - 绿色表示成功，红色表示失败（可查看日志）

---

## 🆘 常见问题

### Q: 部署后网站显示空白？
A: 检查 `vite.config.js` 中的 `base` 路径是否正确

### Q: 修改后没有更新？
A: 
1. 确认代码已推送到GitHub
2. 在Vercel Dashboard查看部署日志
3. 清除浏览器缓存后刷新

### Q: 如何回退到之前的版本？
A: 在Vercel Dashboard中，每个部署都有"Redeploy"按钮，可以重新部署之前的版本

---

## 🎉 完成！

现在你已经知道如何：
- ✅ 部署网站到互联网
- ✅ 修改网站内容
- ✅ 自动更新网站

**记住流程：修改代码 → git push → 自动部署 → 网站更新！**

祝你部署顺利！🚀

