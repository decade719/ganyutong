# 阿里云OSS静态网站托管部署指南

本指南将详细教你如何将Vue项目部署到阿里云OSS静态网站托管服务。

---

## 📋 前置准备

1. **阿里云账号**（如果没有，访问 https://www.aliyun.com 注册）
2. **实名认证**（必须完成实名认证才能使用OSS服务）
3. **本地已构建的项目**（`npm run build` 生成的 `dist` 目录）

---

## 🚀 第一步：开通OSS服务

### 1.1 登录阿里云控制台

1. 访问 **阿里云控制台**：https://oss.console.aliyun.com
2. 使用你的阿里云账号登录
3. 如果还没有实名认证，系统会提示你进行实名认证（个人认证或企业认证）

### 1.2 开通OSS服务

1. 登录后，如果首次使用OSS，系统会引导你开通服务
2. OSS服务通常是**按量付费**，但有**免费额度**：
   - **存储空间**：5GB（标准存储）
   - **流量**：5GB/月（外网下行流量）
   - **请求次数**：每月免费20,000次
3. 对于个人网站，免费额度通常足够使用

---

## 🪣 第二步：创建Bucket（存储桶）

### 2.1 创建Bucket

1. 在OSS控制台页面，点击左上角的 **"创建Bucket"** 按钮
2. 或者点击 **"Bucket列表"** -> **"创建Bucket"**

### 2.2 配置Bucket信息

填写以下信息：

#### **Bucket名称**（重要！）
- 名称要求：
  - 全局唯一（全阿里云唯一）
  - 只能包含小写字母、数字和短横线（-）
  - 长度3-63个字符
  - 必须以小写字母或数字开头和结尾
- 示例：`personal-website-yutong`、`yutong-blog-2024`
- 💡 **提示**：如果提示名称已存在，可以加上你的用户名或日期

#### **地域**
- 选择离你最近的地域，例如：
  - **华东1（杭州）** - 适合华东地区
  - **华北2（北京）** - 适合华北地区
  - **华南1（深圳）** - 适合华南地区
- 💡 **提示**：选择离用户最近的地域可以提升访问速度

#### **存储类型**
- 选择 **"标准存储"**（默认）
- 这是最常用的存储类型，适合频繁访问的网站文件

#### **读写权限**（重要！）
- 选择 **"公共读"**（必须选择这个，否则网站无法访问）
- 说明：
  - **私有**：只有你可以访问
  - **公共读**：所有人可以读取，但只有你可以写入（适合静态网站）
  - **公共读写**：所有人可以读写（不推荐，有安全风险）

#### **版本控制**
- 选择 **"不开启"**（默认即可）

#### **服务端加密**
- 选择 **"不开启"**（默认即可，个人网站通常不需要）

### 2.3 创建Bucket

1. 检查所有配置无误后
2. 点击页面底部的 **"确定"** 按钮
3. 等待几秒钟，Bucket创建成功

---

## 🌐 第三步：开启静态网站托管

### 3.1 进入Bucket管理页面

1. 在OSS控制台的Bucket列表中，找到你刚创建的Bucket
2. 点击Bucket名称，进入Bucket管理页面

### 3.2 开启静态网站托管

1. 在左侧菜单栏，找到 **"基础设置"** 或 **"数据管理"**
2. 点击 **"静态网站托管"**
3. 点击 **"设置"** 或 **"开启"** 按钮

### 3.3 配置静态网站

填写以下配置：

#### **默认首页**
- 填写：`index.html`
- 这是用户访问网站根目录时显示的页面

#### **默认404页**（重要！）
- 填写：`index.html`
- **为什么填index.html？**
  - 因为Vue是单页应用（SPA），所有路由都由前端处理
  - 当用户直接访问 `/about` 等路由时，OSS会返回404
  - 设置404页为 `index.html`，可以让Vue Router处理路由

#### **默认404页（可选）**
- 如果你想要自定义404页面，可以创建一个 `404.html` 文件
- 但SPA应用建议都指向 `index.html`

### 3.4 保存配置

1. 检查配置无误
2. 点击 **"保存"** 按钮
3. 系统会显示静态网站访问地址，格式如：
   ```
   https://你的Bucket名称.oss-地域.aliyuncs.com
   ```
4. 💡 **记住这个地址**，这就是你的网站访问地址！

---

## 📤 第四步：上传网站文件

有两种方式上传文件：**控制台上传**（简单）和 **命令行上传**（推荐，适合后续更新）。

### 方法A：控制台上传（适合首次部署）

#### 4.1 构建项目

在本地项目目录，执行：

```bash
# 确保依赖已安装
npm install

# 构建生产版本
npm run build
```

构建成功后，会在项目根目录生成 `dist` 文件夹。

#### 4.2 上传文件

1. 在OSS控制台的Bucket管理页面
2. 点击左侧菜单的 **"文件管理"** 或 **"对象"**
3. 点击 **"上传"** 按钮
4. 选择上传方式：
   - **方式1：拖拽上传**
     - 直接拖拽 `dist` 文件夹到上传区域
   - **方式2：点击选择**
     - 点击上传区域，选择 `dist` 文件夹
5. 上传设置：
   - **存储类型**：标准存储（默认）
   - **服务端加密**：不开启（默认）
6. 点击 **"上传"** 按钮
7. 等待上传完成（根据文件大小，可能需要几秒到几分钟）

#### 4.3 检查文件结构

上传完成后，确保文件结构正确：

```
Bucket根目录/
├── index.html          ← 必须在根目录
├── assets/
│   ├── index-xxx.js
│   └── index-xxx.css
└── 其他文件...
```

**重要**：确保 `index.html` 在Bucket的根目录，不在 `dist` 子目录中！

---

### 方法B：使用OSS CLI工具上传（推荐，适合后续更新）

#### 4.1 安装OSS CLI工具

**Windows系统：**

1. 访问OSS工具下载页面：https://help.aliyun.com/document_detail/120075.html
2. 下载Windows版本的 `ossutil`（通常是 `.exe` 文件）
3. 解压到任意目录，例如 `C:\ossutil\`
4. 将 `ossutil.exe` 所在目录添加到系统PATH环境变量

**或者使用包管理器安装：**

```powershell
# 使用 Chocolatey（如果已安装）
choco install ossutil

# 或使用 Scoop（如果已安装）
scoop install ossutil
```

**Mac/Linux系统：**

```bash
# 下载
wget http://gosspublic.alicdn.com/ossutil/1.7.14/ossutil64

# 添加执行权限
chmod 755 ossutil64

# 移动到系统路径（可选）
sudo mv ossutil64 /usr/local/bin/ossutil
```

#### 4.2 配置OSS CLI

在命令行执行：

```bash
ossutil config
```

按提示输入：

1. **配置文件路径**：直接回车使用默认路径
2. **语言**：输入 `CH`（中文）或 `EN`（英文）
3. **Endpoint**：输入你的Bucket所在地域的Endpoint
   - 华东1（杭州）：`oss-cn-hangzhou.aliyuncs.com`
   - 华北2（北京）：`oss-cn-beijing.aliyuncs.com`
   - 华南1（深圳）：`oss-cn-shenzhen.aliyuncs.com`
   - 更多地域：https://help.aliyun.com/document_detail/31837.html
4. **AccessKeyId**：输入你的AccessKey ID
   - 如何获取：控制台右上角头像 -> "AccessKey管理" -> "创建AccessKey"
5. **AccessKeySecret**：输入你的AccessKey Secret
6. **STS Token**：直接回车（不使用）

#### 4.3 使用CLI上传文件

创建部署脚本 `deploy-aliyun.ps1`（Windows）或 `deploy-aliyun.sh`（Mac/Linux）：

**Windows PowerShell脚本：**

```powershell
# deploy-aliyun.ps1
Write-Host "🚀 开始部署到阿里云OSS..." -ForegroundColor Green

# 构建项目
Write-Host "📦 构建项目..." -ForegroundColor Cyan
npm run build

if (-not (Test-Path "dist")) {
    Write-Host "❌ 构建失败！" -ForegroundColor Red
    exit 1
}

# 上传到OSS
Write-Host "📤 上传文件到OSS..." -ForegroundColor Cyan
$BUCKET_NAME = "你的Bucket名称"  # 修改为你的Bucket名称
$ENDPOINT = "oss-cn-hangzhou.aliyuncs.com"  # 修改为你的地域Endpoint

ossutil cp -r dist/ oss://$BUCKET_NAME/ --update

if ($LASTEXITCODE -eq 0) {
    Write-Host "✅ 部署成功！" -ForegroundColor Green
    Write-Host "访问地址: https://$BUCKET_NAME.$ENDPOINT" -ForegroundColor Cyan
} else {
    Write-Host "❌ 上传失败！" -ForegroundColor Red
}
```

**Mac/Linux Bash脚本：**

```bash
#!/bin/bash
# deploy-aliyun.sh

echo "🚀 开始部署到阿里云OSS..."

# 构建项目
echo "📦 构建项目..."
npm run build

if [ ! -d "dist" ]; then
    echo "❌ 构建失败！"
    exit 1
fi

# 上传到OSS
echo "📤 上传文件到OSS..."
BUCKET_NAME="你的Bucket名称"  # 修改为你的Bucket名称
ENDPOINT="oss-cn-hangzhou.aliyuncs.com"  # 修改为你的地域Endpoint

ossutil cp -r dist/ oss://$BUCKET_NAME/ --update

if [ $? -eq 0 ]; then
    echo "✅ 部署成功！"
    echo "访问地址: https://$BUCKET_NAME.$ENDPOINT"
else
    echo "❌ 上传失败！"
fi
```

**使用方法：**

```bash
# Windows
.\deploy-aliyun.ps1

# Mac/Linux
chmod +x deploy-aliyun.sh
./deploy-aliyun.sh
```

---

## ✅ 第五步：访问网站

### 5.1 获取访问地址

部署完成后，你的网站访问地址格式为：

```
https://你的Bucket名称.oss-地域.aliyuncs.com
```

例如：
```
https://personal-website-yutong.oss-cn-hangzhou.aliyuncs.com
```

### 5.2 测试访问

1. 在浏览器中打开访问地址
2. 检查网站是否正常显示
3. 测试各个页面和功能

### 5.3 常见问题排查

**问题1：页面显示404或空白**

- 检查 `index.html` 是否在Bucket根目录
- 检查静态网站托管是否已开启
- 检查Bucket读写权限是否为"公共读"

**问题2：CSS/JS文件加载失败**

- 检查文件路径是否正确
- 检查 `vite.config.js` 中的 `base` 配置
- 在浏览器开发者工具中查看网络请求

**问题3：路由刷新后404**

- 确保静态网站托管的404页设置为 `index.html`
- 这是SPA应用必须的配置

---

## 🔄 第六步：后续更新

### 方法A：控制台更新

1. 本地修改代码并构建：`npm run build`
2. 在OSS控制台删除旧文件（可选，直接覆盖也可以）
3. 上传新的 `dist` 目录文件
4. 等待上传完成，刷新网站查看更新

### 方法B：CLI工具更新（推荐）

使用上面创建的部署脚本：

```bash
# Windows
.\deploy-aliyun.ps1

# Mac/Linux
./deploy-aliyun.sh
```

脚本会自动：
1. 构建项目
2. 上传新文件到OSS（只上传有变化的文件）

---

## 🌍 第七步：配置自定义域名（可选）

如果你想使用自己的域名（如 `www.yourname.com`），可以配置自定义域名。

### 7.1 绑定域名

1. 在Bucket管理页面，点击左侧菜单 **"传输管理"** -> **"域名管理"**
2. 点击 **"绑定域名"**
3. 输入你的域名（如 `www.yourname.com`）
4. 选择 **"自动添加CNAME记录"**（如果域名在阿里云解析）
5. 点击 **"确定"**

### 7.2 配置DNS解析

如果域名不在阿里云解析，需要手动配置：

1. 登录你的域名DNS服务商（如阿里云、腾讯云等）
2. 添加CNAME记录：
   - **主机记录**：`www`（或 `@` 表示根域名）
   - **记录类型**：`CNAME`
   - **记录值**：OSS会提供，格式如 `你的Bucket名称.oss-cn-hangzhou.aliyuncs.com`
   - **TTL**：600（默认）

### 7.3 配置HTTPS证书

1. 在域名管理页面，找到你绑定的域名
2. 点击 **"证书托管"** 或 **"HTTPS设置"**
3. 上传SSL证书（可以使用免费的Let's Encrypt证书）
4. 开启HTTPS访问

---

## 💰 费用说明

### 免费额度

- **存储空间**：5GB（标准存储）
- **外网下行流量**：5GB/月
- **请求次数**：20,000次/月

### 超出免费额度后的费用

- **存储费用**：约 0.12元/GB/月
- **流量费用**：约 0.5元/GB
- **请求费用**：约 0.01元/万次

**对于个人网站**：
- 如果网站文件小于5GB
- 每月访问量产生的流量小于5GB
- 基本可以**免费使用**

### 查看费用

1. 在阿里云控制台，点击右上角 **"费用"**
2. 查看OSS服务的详细费用明细

---

## 🛠️ 高级配置

### 配置CDN加速（可选）

如果网站访问量较大，可以配置CDN加速：

1. 在阿里云控制台，开通 **CDN服务**
2. 添加加速域名，源站选择你的OSS Bucket
3. 配置缓存规则
4. CDN可以显著提升访问速度，特别是跨地域访问

### 配置缓存策略

在OSS Bucket的 **"基础设置"** -> **"静态网站托管"** 中，可以配置：

- **HTTP头**：设置缓存时间
- **跨域设置**：如果需要跨域访问

---

## 📝 总结

### 部署流程总结

1. ✅ 开通OSS服务
2. ✅ 创建Bucket（配置为"公共读"）
3. ✅ 开启静态网站托管（404页设为 `index.html`）
4. ✅ 上传 `dist` 目录文件
5. ✅ 访问网站测试
6. ✅ （可选）配置自定义域名

### 后续更新

使用CLI工具或控制台上传新文件即可。

### 需要帮助？

- 阿里云官方文档：https://help.aliyun.com/product/31815.html
- OSS控制台：https://oss.console.aliyun.com
- 技术支持：在控制台提交工单

---

**祝部署顺利！🎉**

