# 国内免费部署指南

本指南提供多个国内免费部署方案，推荐按顺序尝试。

---

## 🎯 方案一：Gitee Pages（推荐，最简单）

**优点：**
- ✅ 完全免费
- ✅ 国内访问速度快
- ✅ 配置简单，支持自动部署
- ✅ 支持自定义域名
- ✅ 无需备案

**缺点：**
- ⚠️ 需要实名认证（码云账号）
- ⚠️ 免费版需要手动触发更新（或使用Gitee Go自动部署）

### 部署步骤

#### 1. 准备代码仓库

```bash
# 如果还没有初始化Git仓库
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit"
```

#### 2. 创建Gitee仓库并推送代码

1. 访问 https://gitee.com 并登录（如果没有账号需要注册）
2. 点击右上角 "+" -> "新建仓库"
3. 填写仓库信息：
   - 仓库名称：例如 `personal-website`
   - 仓库介绍：个人网站
   - 选择"公开"
   - 不要勾选"使用Readme文件初始化这个仓库"
4. 点击"创建"
5. 按照页面提示推送代码：

```bash
# 添加Gitee远程仓库（替换为你的仓库地址）
git remote add origin https://gitee.com/你的用户名/仓库名.git

# 推送代码
git branch -M main
git push -u origin main
```

#### 3. 构建项目

在本地构建项目：

```bash
# 安装依赖（如果还没安装）
npm install

# 构建生产版本
npm run build
```

构建完成后，`dist` 目录就是需要部署的文件。

#### 4. 部署到Gitee Pages

**方法A：手动部署（最简单）**

1. 在Gitee仓库页面，点击"服务" -> "Gitee Pages"
2. 如果没有看到"Gitee Pages"，需要先进行实名认证：
   - 点击右上角头像 -> "设置" -> "账号安全" -> "实名认证"
3. 在Gitee Pages设置页面：
   - 部署分支：选择 `main` 或 `master`
   - 部署目录：填写 `dist`（重要！）
   - 点击"启动"或"更新"
4. 等待部署完成（通常1-2分钟）
5. 部署成功后，会显示访问地址，格式如：`https://你的用户名.gitee.io/仓库名`

**方法B：使用Gitee Go自动部署（推荐）**

1. 在仓库页面，点击"构建" -> "新建流水线"
2. 选择"Vue"模板或"自定义构建"
3. 配置构建步骤：
   ```yaml
   # 构建步骤
   - name: 安装依赖
     script: npm install
   
   - name: 构建项目
     script: npm run build
   
   - name: 部署到Pages
     script: |
       # 将dist目录内容复制到pages目录
       cp -r dist/* pages/
   ```
4. 保存并运行流水线
5. 在"服务" -> "Gitee Pages"中启用自动部署

#### 5. 后续更新

**手动更新：**
```bash
# 修改代码后
git add .
git commit -m "更新内容"
git push

# 然后在Gitee Pages页面点击"更新"按钮
```

**自动更新（使用Gitee Go）：**
```bash
# 推送代码后，Gitee Go会自动构建和部署
git add .
git commit -m "更新内容"
git push
```

---

## 🎯 方案二：腾讯云静态网站托管

**优点：**
- ✅ 国内访问速度快
- ✅ 免费额度充足（50GB存储，10GB流量/月）
- ✅ 支持CDN加速
- ✅ 支持自定义域名

**缺点：**
- ⚠️ 需要实名认证
- ⚠️ 需要腾讯云账号

### 部署步骤

#### 1. 开通服务

1. 访问 https://console.cloud.tencent.com/webify
2. 登录腾讯云账号（需要实名认证）
3. 进入"静态网站托管"服务
4. 点击"立即使用"或"创建环境"

#### 2. 创建静态网站

1. 点击"新建静态网站"
2. 填写网站信息：
   - 网站名称：例如 `personal-website`
   - 环境名称：自动生成
3. 点击"创建"

#### 3. 上传文件

**方法A：控制台上传**

1. 在本地构建项目：
   ```bash
   npm run build
   ```
2. 在腾讯云控制台，进入你的静态网站
3. 点击"文件管理" -> "上传文件"
4. 选择 `dist` 目录下的所有文件上传
5. 确保 `index.html` 在根目录

**方法B：使用CLI工具（推荐）**

1. 安装腾讯云CLI：
   ```bash
   npm install -g @cloudbase/cli
   ```
2. 登录：
   ```bash
   tcb login
   ```
3. 初始化项目：
   ```bash
   tcb init
   ```
4. 部署：
   ```bash
   npm run build
   tcb hosting deploy dist -e 你的环境ID
   ```

#### 4. 访问网站

部署完成后，会获得一个访问地址，格式如：
`https://你的环境ID-你的网站ID.tcloudbaseapp.com`

#### 5. 配置自定义域名（可选）

1. 在静态网站管理页面，点击"域名管理"
2. 添加自定义域名
3. 按照提示配置DNS解析

---

## 🎯 方案三：阿里云OSS静态网站托管

**优点：**
- ✅ 国内访问速度快
- ✅ 免费额度（5GB存储，5GB流量/月）
- ✅ 支持CDN加速
- ✅ 支持自定义域名

**缺点：**
- ⚠️ 需要实名认证
- ⚠️ 需要阿里云账号
- ⚠️ 免费额度较小

### 部署步骤

#### 1. 开通OSS服务

1. 访问 https://oss.console.aliyun.com
2. 登录阿里云账号（需要实名认证）
3. 点击"创建Bucket"

#### 2. 创建Bucket

1. 填写Bucket信息：
   - Bucket名称：例如 `personal-website`（需全局唯一）
   - 地域：选择离你最近的地域
   - 存储类型：标准存储
   - 读写权限：公共读
2. 点击"确定"

#### 3. 开启静态网站托管

1. 进入创建的Bucket
2. 点击"基础设置" -> "静态网站托管"
3. 点击"设置"
4. 设置：
   - 默认首页：`index.html`
   - 默认404页：`index.html`（SPA应用需要）
5. 点击"保存"

#### 4. 上传文件

**方法A：控制台上传**

1. 在本地构建项目：
   ```bash
   npm run build
   ```
2. 在OSS控制台，进入你的Bucket
3. 点击"文件管理" -> "上传"
4. 选择 `dist` 目录下的所有文件上传
5. 确保 `index.html` 在根目录

**方法B：使用OSS CLI工具**

1. 安装OSS CLI：
   ```bash
   # Windows
   # 下载安装包：https://help.aliyun.com/document_detail/120075.html
   
   # Mac/Linux
   wget http://gosspublic.alicdn.com/ossutil/1.7.14/ossutil64
   chmod 755 ossutil64
   ```
2. 配置：
   ```bash
   ossutil config
   ```
3. 上传：
   ```bash
   npm run build
   ossutil cp -r dist/ oss://你的Bucket名称/ --update
   ```

#### 5. 访问网站

在"静态网站托管"页面，会显示访问地址，格式如：
`https://你的Bucket名称.oss-地域.aliyuncs.com`

#### 6. 配置自定义域名（可选）

1. 在Bucket管理页面，点击"传输管理" -> "域名管理"
2. 绑定自定义域名
3. 按照提示配置DNS解析和HTTPS证书

---

## 🎯 方案四：Coding Pages（腾讯云CODING）

**优点：**
- ✅ 完全免费
- ✅ 国内访问速度快
- ✅ 支持自动部署
- ✅ 支持自定义域名

**缺点：**
- ⚠️ 需要实名认证
- ⚠️ 需要CODING账号

### 部署步骤

#### 1. 创建CODING项目

1. 访问 https://coding.net
2. 登录CODING账号（需要实名认证）
3. 创建新项目

#### 2. 推送代码

```bash
# 添加CODING远程仓库
git remote add coding https://e.coding.net/你的团队名/项目名.git

# 推送代码
git push -u coding main
```

#### 3. 配置持续部署

1. 在CODING项目页面，进入"持续部署" -> "静态网站"
2. 点击"新建静态网站"
3. 配置：
   - 代码仓库：选择你的仓库
   - 构建命令：`npm install && npm run build`
   - 构建产物目录：`dist`
4. 点击"保存并部署"

#### 4. 访问网站

部署完成后，会获得一个访问地址，格式如：
`https://你的项目名.coding-pages.com`

---

## 📝 通用配置说明

### 1. 修改Vite配置（如果需要子路径部署）

如果你的网站部署在子路径下（如 `https://example.com/website/`），需要修改 `vite.config.js`：

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  base: '/', // 如果部署在子路径，改为 '/子路径/'
  // ...其他配置
})
```

### 2. 构建项目

所有方案都需要先构建项目：

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build
```

构建完成后，`dist` 目录就是需要部署的文件。

### 3. 预览构建结果

在部署前，可以本地预览：

```bash
npm run preview
```

---

## 🚀 快速开始推荐

**如果你是第一次部署，推荐使用方案一（Gitee Pages）：**

1. ✅ 最简单，配置最少
2. ✅ 完全免费
3. ✅ 国内访问速度快
4. ✅ 支持自动部署（使用Gitee Go）

**如果Gitee Pages不满足需求，可以尝试：**
- 需要更多流量和存储：方案二（腾讯云静态网站托管）
- 需要CDN加速：方案二或方案三（阿里云OSS）
- 需要CI/CD自动化：方案四（Coding Pages）

---

## ❓ 常见问题

### Q1: 部署后页面空白？

**A:** 检查以下几点：
1. 确保 `index.html` 在根目录
2. 检查资源路径是否正确（CSS、JS文件是否能正常加载）
3. 如果是SPA应用，确保服务器配置了将所有路由指向 `index.html`

### Q2: 如何配置自定义域名？

**A:** 
- Gitee Pages：在"Gitee Pages"设置页面绑定域名，然后配置DNS解析
- 腾讯云/阿里云：在各自的域名管理页面绑定域名并配置DNS

### Q3: 如何实现自动部署？

**A:**
- Gitee Pages：使用Gitee Go配置CI/CD
- Coding Pages：自动支持，推送代码即自动部署
- 腾讯云/阿里云：可以配置GitHub Actions或GitLab CI

### Q4: 部署后样式丢失？

**A:** 检查 `vite.config.js` 中的 `base` 配置是否正确，确保资源路径正确。

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 查看各平台的官方文档
2. 检查浏览器控制台的错误信息
3. 确认构建是否成功（`npm run build` 无错误）

---

**祝部署顺利！🎉**

